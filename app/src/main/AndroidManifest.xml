<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="co.edu.sena.projectupmobile">
    <!-- Permisos -->
    <!-- check network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_MEDIA_STORAGE"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- Camera permission -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- File read permission -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono_principal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectUpMobile">
        <activity
            android:name=".Scan_Activity"
            android:exported="false" />
        <!-- Inicio SplashActivity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ProjectUpMobile.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Fin SplashActivity -->
        <!-- Inicio MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- Fin MainActivity -->
        <!-- Inicio crearEvento_Activity -->
        <activity
            android:name=".crearEvento_Activity"
            android:exported="false"
            android:parentActivityName=".HomeActivity" /> <!-- Fin crearEvento_Activity -->
        <!-- Inicio HomeActivity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" /> <!-- Inicio HomeActivity -->
        <!-- Inicio NavegationActivity -->
        <activity
            android:name=".NavegationActivity"
            android:exported="true"
            android:theme="@style/Theme.ProjectUpMobile.NoActionBar" />
    </application>

</manifest>